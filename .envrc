# SPDX-FileCopyrightText: © 2023 Tyler Nivin
# SPDX-License-Identifier: MIT

# Activate the uv-managed virtualenv when present.
if [ -d ".venv" ]; then
  export VIRTUAL_ENV="$PWD/.venv"
  PATH_add .venv/bin
fi
